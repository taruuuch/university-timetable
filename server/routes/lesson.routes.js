// const express = require('express');
// const lessonController = require('../controllers/lesson.controller');
// const authMiddleware = require('../middleware/auth.middleware');

// const router = express.Router();

// router.get('/', authMiddleware, lessonController.getLessons);
// router.post('/', authMiddleware, lessonController.createLesson);
// router.get('/:lessonId', authMiddleware, lessonController.getLessonById);
// router.patch('/:lessonId', authMiddleware, lessonController.updateLesson);
// router.delete('/:lessonId', authMiddleware, lessonController.deleteLesson);

// module.exports = router;
