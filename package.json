{
  "name": "university-timetable",
  "version": "0.0.1",
  "main": "server.js",
  "repository": "https://github.com/taruuuch/university-timetable.git",
  "author": "Artemchuk Dmytro <taruuuch@gmail.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
		"client": "yarn --cwd client start",
    "server": "yarn --cwd server nodemon -q server.js",
    "migration": "yarn --cwd server migration",
		"start": "echo \"Error: no prod version specified\" && exit 1",
    "dev": "concurrently \"yarn server\" \"yarn client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "concurrently": "^5.1.0"
  }
}
